# Phantom container-scan findings — TEMPORARY ignore, revisit and remove.
#
# The backend image actually installs PATCHED versions: setuptools 84.0.0 and
# msgpack 1.2.2 (both scan 0 vulns — confirmed by the container-security
# diagnostic table step). Trivy nonetheless reports stale metadata for versions
# that server/backend/Dockerfile never installs (setuptools 70.3.0, msgpack
# 1.1.2), i.e. ghost *.dist-info from some second location in the image that
# has not yet been located (needs `docker run ... find /` on the built image).
#
# Ignoring so the fail-closed gate reflects real risk (none — patched) instead
# of the phantom. TODO: locate the ghost path, remove it from the image, then
# delete this file so these advisories are enforced again.
CVE-2025-47273
GHSA-6v7p-g79w-8964
